fastapi==0.115.0
uvicorn[standard]==0.30.6
celery[redis]==5.4.0
redis==5.1.1
pydantic==2.9.2

# ── Script generation (Groq — free tier) ─────────────────────────────────────
groq==0.11.0

# ── TTS (edge-tts — Microsoft Edge neural, 100% free, no API key) ────────────
edge-tts==6.1.12

# ── Image generation (HuggingFace Inference API — free tier) ─────────────────
httpx==0.27.2
aiohttp==3.10.10

# ── Timestamps (faster-whisper — local, free) ────────────────────────────────
faster-whisper==1.0.3

# ── Video / image processing ─────────────────────────────────────────────────
moviepy==1.0.3
Pillow==10.4.0
imageio==2.35.1
imageio-ffmpeg==0.5.1
numpy==1.26.4

# ── Utilities ─────────────────────────────────────────────────────────────────
aiofiles==24.1.0
python-multipart==0.0.12
python-dotenv==1.0.1
tenacity==9.0.0
